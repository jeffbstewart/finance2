{
  "name": "finance2-web",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript side of the finance2 proto contract. Phase 6 replaces this minimal workspace with the real SPA; the codegen + compile check is real from day one.",
  "scripts": {
    "proto:gen": "node scripts/gen-proto.mjs",
    "precheck": "npm run proto:gen",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "@connectrpc/connect": "^2.1.1",
    "@connectrpc/connect-web": "^2.1.1"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "^2.12.0",
    "protoc": "^35.1.0",
    "typescript": "~5.9.3"
  }
}
